### Что будет:
- **FastAPI** — REST API
- **PostgreSQL** — база данных
- **Redis** — кэширование
- **RabbitMQ** — брокер сообщений
- **Docker + Docker Compose**
- **Асинхронность**, обработка 10+ RPS
- **Один публичный метод**: `POST /shorten` → возвращает короткую ссылку